{
  "name": "horse-cargo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Horse Cargo operating system — web app (www/) + Android APK via Capacitor",
  "scripts": {
    "serve": "npx --yes http-server www -p 8080 -c-1",
    "cap:sync": "npx cap sync android",
    "apk:debug": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "apk:release": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
